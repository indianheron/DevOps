---
- hosts: webservers
  tasks:
    - name: Copy the files to remote machines 
      become: true
      copy:
        src: ~/test_html01/index.html
        dest: /var/www/html
        owner: www-data
        group: www-data
        mode: 0644
